routes:
  - intent: filesystem
    tools: [list_files, read_file, write_file, edit_file, glob_files, grep_files]
  - intent: code_execution
    tools: [run_command]
  - intent: web_research
    tools: [fetch_url]
  - intent: api_call
    tools: [http_request]
  - intent: memory
    tools: [remember, recall]
